generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Mood {
  id          Int      @id @default(autoincrement())
  author      String
  mood        Int
  habits      String[]
  content     String
  publishedAt DateTime @default(now())
}